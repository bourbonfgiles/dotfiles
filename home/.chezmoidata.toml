# Committed defaults (personal). Work identity is NOT stored here — set it on the
# machine under ~/.config/chezmoi/chezmoi.toml (see chezmoi.toml.example).
[git]
name = "bourbonfgiles"
email = "giles@live.co.uk"
